Config = {}

-- Discord webhook for the exit-log channel (paste yours; keep the channel STAFF-ONLY)
Config.Webhook = 'PUT_YOUR_DISCORD_WEBHOOK_HERE'

-- distance (meters) that counts as "nearby" when someone disconnects
Config.NearbyRadius = 30.0

-- embed look
Config.BotName = 'NAIJA 2046 | Exit Log'
Config.Color   = 15158332  -- red
Config.Avatar  = ''         -- optional image url

-- refresh how often (seconds) we cache player positions (used to find nearby on drop)
Config.PosInterval = 5
